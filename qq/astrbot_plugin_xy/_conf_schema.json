{
  "owner_qq": {
    "description": "主人 QQ 号（唯一可私聊小月的账号，其余一律不理会）",
    "type": "string",
    "hint": "填写你的 QQ 号，如 10001。群聊消息无论谁发一律静默。",
    "obvious_hint": true,
    "default": "",
    "restart_required": false
  },
  "api_base": {
    "description": "小月服务地址",
    "type": "string",
    "hint": "小月 FastAPI 服务的根地址，同机部署用 http://127.0.0.1:8000",
    "obvious_hint": true,
    "default": "http://127.0.0.1:8000",
    "restart_required": false
  },
  "api_token": {
    "description": "小月服务 API Token",
    "type": "string",
    "hint": "与服务器 .env 的 API_TOKEN 一致，用于 Bearer 鉴权",
    "obvious_hint": true,
    "default": "",
    "restart_required": false
  },
  "identity_secret": {
    "description": "QQ 用户身份签名密钥",
    "type": "string",
    "hint": "必须与服务器 .env 的 QQ_IDENTITY_SECRET 一致；QQ_API_TOKEN 启用时用于防止伪造 user_id",
    "default": "",
    "restart_required": false
  },
  "onebot_http": {
    "description": "NapCat onebot HTTP 地址（文件会话下载通道）",
    "type": "string",
    "hint": "同机 NapCat onebot HTTP 服务，如 http://127.0.0.1:3100；留空则文件下载退回 URL 直连/代理",
    "obvious_hint": true,
    "default": "http://127.0.0.1:3100",
    "restart_required": false
  },
  "onebot_token": {
    "description": "NapCat onebot HTTP Token",
    "type": "string",
    "hint": "与 NapCat onebot httpServers 配置的 token 一致",
    "obvious_hint": true,
    "default": "",
    "restart_required": false
  },
  "download_proxy": {
    "description": "文件下载代理（QQ CDN 直连 502 时的兜底通道）",
    "type": "string",
    "hint": "如 http://127.0.0.1:7890（clash）；留空 = 不走代理直连",
    "obvious_hint": true,
    "default": "",
    "restart_required": false
  },
  "container_path_map": {
    "description": "NapCat 容器路径 → 宿主机路径映射",
    "type": "string",
    "hint": "格式：容器前缀=宿主机前缀，多对用分号分隔；与部署的挂载点一致",
    "obvious_hint": true,
    "default": "/app/.config/QQ/=/opt/napcat/qq_config/;/app/napcat/cache/=/opt/napcat/cache/;/app/napcat/config/=/opt/napcat/config/",
    "restart_required": false
  },
  "vision_max_image_bytes": {
    "description": "视觉图片大小上限（字节）",
    "type": "int",
    "hint": "仅接受 JPEG/PNG/WebP；默认 10485760（10MB），下载和上传前都会限制",
    "default": 10485760,
    "restart_required": false
  },
  "vision_timeout": {
    "description": "视觉请求超时（秒）",
    "type": "number",
    "hint": "图片下载、NapCat 取图和 /api/chat/vision 使用独立超时；默认 90 秒",
    "default": 90,
    "restart_required": false
  }
}
